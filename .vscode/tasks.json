{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "python3 -m ${file}", // WRONG, move setup.py to args
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "args": [
              
            ],
            "presentation": {
                "echo": true,
                "panel": "shared",
                "focus": true
            }
        }
    ]
}
